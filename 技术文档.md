# 露米绘画AI 技术文档

## 1. 技术栈

- 小程序：uni-app，Vue 3，TypeScript，微信小程序目标平台。
- 后端：NestJS，Node.js，TypeScript。
- 管理后台：React，Ant Design Pro，TypeScript，独立 Web 项目。
- 数据库：PostgreSQL。
- 缓存与队列：Redis，BullMQ。
- 对象存储：阿里云 OSS，bucket 为 `lumidraw`，区域为 `oss-cn-beijing`。
- AI 平台：KIE.AI。
- 支付：微信支付。
- 部署：docker-compose。

## 2. Monorepo 规划

后续目录规划：

- `apps/mobile`：微信小程序。
- `apps/api`：NestJS API 服务，在服务器 Remote SSH 阶段创建和开发。
- `apps/admin`：管理后台。
- `packages/shared`：跨端共享类型、常量和接口契约。
- `prototype`：原型文件，仅作为迁移和验收依据。
- `docs` 或根目录文档：产品、技术和 Spec 文档。

第一轮不创建脚手架，仅建立文档和安全规则。

开发阶段顺序：

1. 本地 Codex：开发 `apps/mobile` 小程序前端。
2. 本地 Codex：开发 `apps/admin` 管理后台前端。
3. 用户使用 VS Code Remote SSH 打开服务器工作区。
4. 服务器 Codex 插件：创建并开发 `apps/api` 后端。
5. 服务器环境完成前后端联调和 docker-compose 部署。

## 3. 配置与密钥

- 私有配置只放 `agent.md` 或 `.env*`，并由 `.gitignore` 忽略。
- 可提交代码和文档只使用变量名，不写真实密钥。
- 后端启动时从环境变量读取微信、KIE、OSS、支付、数据库、Redis 配置。
- 生产环境由服务器环境变量或部署平台注入，不从 Git 仓库读取真实密钥。

关键变量名：

- 微信：`WX_APPID`、`WX_APPSECRET`。
- KIE：`KIE_API_BASE`、`KIE_API_KEY`、`KIE_CALLBACK_URL`。
- OSS：`OSS_ACCESS_KEY_ID`、`OSS_ACCESS_KEY_SECRET`、`OSS_BUCKET`、`OSS_ENDPOINT`。
- 微信支付：`WX_MCH_ID`、`WX_PAY_API_V3_KEY`、`WX_PAY_CERT_SERIAL_NO`、`WX_PAY_PRIVATE_KEY`/`WX_PAY_PRIVATE_KEY_PATH`、`WX_PAY_PLATFORM_CERTIFICATE`/`WX_PAY_PLATFORM_CERTIFICATE_PATH`、`WX_PAY_NOTIFY_URL`。

## 4. 小程序技术边界

- 登录只使用微信静默登录：小程序调用 `wx.login`，后端使用 code 换取 openid/session。
- 小程序侧保留 mock 数据开关，开关开启时所有页面从本地 mock 服务层读取数据。
- 小程序页面只通过统一 API client 或 mock client 获取数据，页面内不散落请求逻辑。
- 本地前端阶段优先完善 mock 数据闭环；后端未完成前，不阻塞小程序界面验收。
- 原型中的图标后续迁移为本地化 iconfont/uni-icons 方案，不依赖运行时 CDN。
- 图片上传通过后端签名或后端代理进入 OSS，避免小程序端持有 OSS 密钥。

## 5. 后端模块规划

后端不在本地 Codex 前端阶段实现。当前文档只定义后端边界；实际 NestJS 工程、数据库、队列、OSS、KIE、微信支付和接口联调在服务器 Remote SSH 阶段完成。

- Auth：微信静默登录、会话、用户初始化。
- Users：用户资料、关注、粉丝、状态管理。
- Works：作品、草稿、发布、编辑、浏览、点赞、收藏、举报。
- Engine：生图引擎 v3。任务门面 /engine/*、协议适配器注册表、故障转移、看门狗推进、三段计费与退款、图片转存。
- Assets：图片上传、OSS 访问、临时 URL、资源归档。
- Credits：积分余额、扣减、奖励、流水。
- Payments：微信支付订单、回调、退款预留。
- Membership：会员方案、权益和有效期。
- Checkin：签到、连续签到和里程碑奖励。
- Invite：邀请关系和奖励。
- Review：微信内容审核、人工审核开关、审核记录。
- Ops：轮播、玩法、风格、分类、热搜、模型、比例、精度配置。
- Messages：系统通知、互动消息、审核消息、公告。
- Feedback：用户反馈、处理状态和回复。
- Admin：后台身份、权限和运营 API。

## 6. 生图上游接入（适配器体系）

协议差异收敛在 `engine/adapters` 注册表，业务代码只面向统一接口：

- `openai-images`：OpenAI /v1/images/generations、/v1/images/edits 同步协议及兼容聚合平台。
- `gemini`：Gemini :generateContent 协议，参考图内联 base64。
- `kie`：KIE createTask/recordInfo 异步任务协议，支持回调。创建任务 `POST /api/v1/jobs/createTask`，查询 `GET /api/v1/jobs/recordInfo?taskId=...`，鉴权 `Authorization: Bearer <KIE_API_KEY>`。
- `async-http`：请求模板 + 响应映射的通用 HTTP 协议，接入新平台无需写代码。

平台配置的唯一权威是 `GenerationProvider.config` JSON（管理端 /admin/engine/* 维护）；v3.1 起旧的平面配置列已删除，`config` 为 NOT NULL 单轨，读取时与协议默认值合并归一。任务提交时把可用配置快照进 EngineJob，配置后续变更不影响进行中任务。

管理端提供「全链路试运行」（`POST /admin/engine/platforms/:id/dry-run`）：用系统用户建一条 `dryRun=true` 的真实任务，走完提交 → 上游 → FC 转存/直存 → OSS → CDN，0 积分、不建草稿作品、不计入平台健康度，watchdog 3 天后自动清理。`GET /admin/engine/dry-runs/:jobId` 返回阶段化进度（attempts 耗时、assets 转存指标、CDN 图片 URL）。新平台配置后应先试运行验证再挂入模型线路。

看门狗常量：提交超时 2 分钟、总时长 35 分钟、轮询 20 秒、失败快速重试窗口 15 秒、每平台最多 2 次尝试。可能已被上游计费的请求绝不原地重试。图片字节永不经过小带宽 API 服务器：URL 结果由 FC 下载转存 OSS，base64/同步结果整条生成派发进 FC 直存 OSS；FC 未配置时进程内转存仅作为兜底，生产环境启动即 error 告警。

## 7. 数据与队列

- PostgreSQL 存储用户、作品、订单、积分流水、生成任务、配置和审核记录。
- Redis 用于会话缓存、限流、任务状态缓存和 BullMQ。
- 生成任务的推进由进程内看门狗（20 秒 tick）完成：孤儿任务恢复、卡死提交清理、到期轮询、总超时、僵尸结算清理与钱包退款补偿；不再依赖 BullMQ。
- 所有积分扣减和订单状态变更必须由后端事务或幂等逻辑保护。

## 8. 管理后台技术边界

- 管理后台为独立 Web 项目，不嵌入小程序工程。
- 后台通过同一 NestJS API 交互。
- 列表、详情、编辑、启停、审核、通知等能力按原型模块逐步迁移。
- 管理后台设置页必须提供模拟数据开关。
- mock 开关开启时，后台所有页面使用本地 mock 数据并能独立验收前端流程。
- mock 开关关闭后，后台只使用真实 API，用于服务器后端联调和生产验证。

## 9. 部署规划

docker-compose 服务规划：

- `api`：NestJS 后端。
- `admin`：管理后台静态站点或 Web 服务。
- `postgres`：数据库。
- `redis`：缓存与队列。
- `nginx`：域名入口、HTTPS、静态资源和 API 反向代理。

公网域名：

- API 回调路径（服务端间，engine v3）：`https://ejoyflie.cloud/api/engine/callbacks/kie`（KIE 任务回调，附 per-job HMAC 签名）、`/api/engine/callbacks/transfer`（FC 转存完成）、`/api/engine/callbacks/generation`（FC 生成完成与进度）。
- 阿里云 FC 函数环境变量 `API_CALLBACK_URL`/`GENERATION_CALLBACK_URL` 已于 2026-09-18 更新为上述 transfer/generation 新路径并经全链路试运行验证；期间的 nginx 旧路径临时桥接已移除（见 `infra/ainb-image-transfer/README.md`）。
- 后续部署时小程序 API、管理后台和静态资源路径由 nginx 统一转发。
- 前端验收阶段可先部署管理后台静态产物和小程序预览/H5 调试产物；后端 API 在服务器 Remote SSH 阶段接入。

## 10. 外部文档参考

- KIE GPT Image 2 文生图：https://docs.kie.ai/market/gpt/gpt-image-2-text-to-image
- KIE GPT Image 2 图生图：https://docs.kie.ai/cn/market/gpt/gpt-image-2-image-to-image
- KIE Nano Banana 2：https://docs.kie.ai/cn/market/google/nanobanana2
- KIE Nano Banana Pro：https://docs.kie.ai/cn/market/google/pro-image-to-image
- KIE Seedream 4.5：https://docs.kie.ai/cn/market/seedream/4-5-text-to-image
- KIE 查询任务：https://docs.kie.ai/market/common/get-task-detail
- uni-icons / iconfont：https://uniapp.dcloud.net.cn/component/uniui/uni-icons.html
